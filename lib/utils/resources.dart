

// ///Converts between dart's datetime object and jumia's string representation of dates
// ///if [date] has a type of String, a DateTime conversion is done; and vice versa
// dynamic parseDateTime(dynamic date){
//   if (date.runtimeType == String) { //parse as a DateTime object
//
//   } else if (date.runtimeType == DateTime) {//parse as a String object
//
//   }
// }